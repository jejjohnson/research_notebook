defaults:
  - model: baseline
  - data: small
  - _self_

training:
  lr: 1.0e-3
  epochs: 100
  seed: 42
  batch_size: 32
  val_interval: 10

logging:
  dir: outputs/
  log_every_n_steps: 10
  save_checkpoints: true
